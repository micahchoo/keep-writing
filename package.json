{
  "name": "keep-writing",
  "version": "0.1.0",
  "description": "Obsidian plugin: the vault interviews its owner. Draws Asks from the Bank, marks answers done, links blocks by typed relation.",
  "main": "main.js",
  "private": true,
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "test": "bun test"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "bun-types": "^1.3.0",
    "esbuild": "^0.25.0",
    "obsidian": "latest",
    "typescript": "^5.6.0"
  }
}
